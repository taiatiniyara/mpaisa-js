---
"mpaisa-js": minor
---

Core SDK: handshake sessions, redirect verification (`verifyTokenV2`, `confirmRedirect`), status polling with exponential backoff, bearer-token auth with cache hook, secret redaction, and full error hierarchy.
